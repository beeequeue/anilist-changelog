on:
  workflow_dispatch:
  schedule:
    - cron: "*/1 * * * *"

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: BeeeQueue/anilist-changelog@dist

      - run: git status
